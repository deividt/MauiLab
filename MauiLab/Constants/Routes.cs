using MauiLab.Views;

namespace MauiLab.Constants;

public static class Routes
{
    public const string Navigation = nameof(NavigationPage);
    public const string Default = nameof(DefaultPage);
    public const string Main = nameof(MainPage);
}